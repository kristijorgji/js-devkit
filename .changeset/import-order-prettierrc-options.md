---
'@kristijorgji/eslint-config-typescript': minor
'@kristijorgji/eslint-config-react-typescript': major
---

Add `importOrder`, `sortImports`, and `prettier: 'prettierrc'` options (plus `createImportOrderRules`) so consumers can match repo Prettier and import style without local rule overrides.
