export function a(x: number): number {
  const y = x + 1;
  const z = y * 2;
  if (z > 10) {
    return z - 1;
  }
  return z;
}

export function b(x: number): number {
  const y = x + 1;
  const z = y * 2;
  if (z > 10) {
    return z - 1;
  }
  return z;
}
